"use client";
// This file is kept for legacy compatibility.
// The actual layout wrapping is done in src/app/layout.tsx
// This component is no longer used directly.
export default function Layout({ children }) {
  return <>{children}</>;
}